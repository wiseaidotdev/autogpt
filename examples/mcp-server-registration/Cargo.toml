[package]
name = "mcp-server-registration"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1.52.2", features = ["full"] }
autogpt = { path = "../../autogpt", default-features = false, features = ["gem", "mcp"] }
